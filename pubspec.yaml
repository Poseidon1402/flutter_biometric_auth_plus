name: flutter_biometric_auth_plus
description: Advanced biometric authentication plugin with comprehensive Android support including fingerprint, face recognition, iris scanning, and device credentials using modern BiometricPrompt API.
version: 1.0.0
homepage: https://github.com/Poseidon1402/flutter_biometric_auth_plus
repository: https://github.com/Poseidon1402/flutter_biometric_auth_plus
issue_tracker: https://github.com/Poseidon1402/flutter_biometric_auth_plus/issues

topics:
  - biometric
  - authentication
  - fingerprint
  - face-recognition
  - security

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: mg.nexus.flutter_biometric_auth_plus
        pluginClass: FlutterBiometricAuthPlusPlugin
